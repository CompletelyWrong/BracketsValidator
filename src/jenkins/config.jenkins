pipeline {
    agent { label 'Agent lp-public-jnlp-1' }
    stages {
        stage('Git Checkout') {
            steps {
                git url: 'https://github.com/CompletelyWrong/BracketsValidator'
            }
        }
        stage('Build') {
            steps {
                sh "mvn clean install"
            }
        }
    }
}